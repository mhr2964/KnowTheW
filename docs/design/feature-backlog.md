# Feature backlog — unscheduled ideas

Durable holding area for ideas that have been discussed but not designed or scheduled. Unlike the rest of `docs/design/`, entries here are pre-design — move an entry to its own doc (and add it to `design.md`'s index) once it's actually being built, and delete it from this file at that point.

## From the 2026-08-17 BallDontLie GOAT-tier brainstorm

The user has a paid BallDontLie GOAT tier ($39.99/mo, 600 req/min, WNBA coverage at `wnba.balldontlie.io`). Player props was explicitly deferred by the user ("skip the props for now") — the only idea from this brainstorm not carried forward.

Everything else from this brainstorm (injury report, odds/spread, plus a much larger 2026-08-21 follow-up audit of BDL's full endpoint surface — standings, site nav rework, clutch/scoring/usage/defense dashboards, team four factors, team shot chart, league shot-zone leaderboards, per-game advanced stats) moved to its own design doc once scheduled: see `bdl-expansion-roadmap.md`.
